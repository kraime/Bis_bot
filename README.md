# Telegram Business Matching Bot

Телеграм-бот для бизнес-нетворкинга с функциями профилирования участников и подбора релевантных контактов.

## Функциональность

### 🚀 Онбординг новых участников
- Автоматическое приветствие при команде `/start`
- Сбор профиля через 3 вопроса:
  1. Сфера деятельности
  2. Что ищете в сообществе
  3. Чем можете помочь другим
- Опциональный сбор номера телефона
- Сохранение профиля в SQLite + векторные представления в Qdrant

### 🔍 Подбор релевантных участников
- Семантический поиск с использованием Qdrant
- Анализ совместимости через DeepSeek LLM
- Рекомендации с объяснением причин совпадения
- Топ-5 наиболее подходящих контактов

### 📅 Ежемесячные обновления
- Автоматические напоминания об обновлении профиля
- Сохранение истории изменений
- Гибкая настройка интервалов

## Технологический стек

- **Язык:** Python 3.8+
- **База данных:** SQLite (основные данные) + Qdrant (векторный поиск)
- **Эмбеддинги:** all-MiniLM-L6-v2 (SentenceTransformers)
- **LLM:** DeepSeek API
- **Telegram:** aiogram 3.x
- **Логирование:** loguru

## Установка и настройка

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка Qdrant

#### Вариант A: Локальный Qdrant
```bash
# Скачайте Qdrant с https://github.com/qdrant/qdrant/releases
# Запустите: qdrant
# Qdrant будет доступен на http://localhost:6333
```

#### Вариант B: Docker Qdrant
```bash
docker run -p 6333:6333 qdrant/qdrant
```

### 3. Настройка переменных окружения

Создайте файл `.env`:

```bash
# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Database Configuration
DATABASE_PATH=business_bot.db

# Qdrant Configuration
QDRANT_URL=http://localhost:6333
# QDRANT_API_KEY=your_api_key_here  # Только для облачного Qdrant

# DeepSeek API Configuration
DEEPSEEK_API_KEY=your_deepseek_api_key_here
DEEPSEEK_BASE_URL=https://api.deepseek.com

# Bot Configuration
ADMIN_USER_ID=your_telegram_user_id
UPDATE_INTERVAL_DAYS=30
```

### 4. Инициализация баз данных

```bash
python setup_qdrant.py
```

### 5. Запуск бота

```bash
python main.py
```

## Архитектура

### Структура баз данных

**SQLite (business_bot.db):**
- **users** - основная информация о пользователях
- **user_profiles** - профили с ответами на вопросы
- **profile_history** - история изменений профилей
- **user_states** - состояния диалогов для conversation flow

**Qdrant (векторная БД):**
- **user_profiles** коллекция - векторные представления профилей
- Семантический поиск по эмбеддингам
- Метаданные профилей для фильтрации

### Компоненты системы

1. **src/database.py** - работа с SQLite
2. **src/vector_db.py** - интеграция с Qdrant
3. **src/embeddings.py** - создание векторных представлений
4. **src/llm_service.py** - интеграция с DeepSeek для анализа совместимости
5. **src/bot.py** - основная логика Telegram бота
6. **src/text_processing.py** - обработка и очистка текста

### Алгоритм подбора участников

1. **Поиск по ключевым словам** - быстрый поиск в SQLite
2. **Векторный поиск** - семантическое сходство через Qdrant
3. **LLM анализ** - DeepSeek оценивает совместимость и взаимодополняемость
4. **Ранжирование** - сортировка по релевантности
5. **Форматирование** - генерация понятных объяснений

## Команды бота

- `/start` - начало работы / создание профиля
- `/match` - поиск подходящих участников
- **Кнопки меню:**
  - 🔍 Подобрать участников
  - 📝 Обновить анкету  
  - 👤 Мой профиль

## Тестирование

```bash
# Простой тест системы
python test_bot_simple.py

# Полный тест с созданием профилей
python test_full_system.py

# Комплексный тест с матчингом
python test_profiles_matching.py
```

## Логирование

Проект использует **loguru** для красивого логирования:

- **Консоль** - цветные логи с эмодзи-индикаторами
- **Файлы** - детальные логи в `logs/bot_YYYY-MM-DD.log`
- **Ротация** - новый файл каждый день, хранение 7 дней
- **Сжатие** - автоматическое архивирование старых логов

## Масштабирование

### Оптимизация производительности
- Индексы в SQLite для быстрого поиска
- Векторные индексы в Qdrant (HNSW)
- Кэширование эмбеддингов
- Батчевая обработка обновлений

### Мониторинг
- Подробное логирование всех операций
- Метрики использования API
- Отслеживание качества рекомендаций

## Развитие

### Планируемые функции
- Групповые рекомендации
- Интеграция с календарем для встреч
- Система рейтингов и отзывов
- Аналитика эффективности знакомств

### Настройка алгоритмов
- Веса для разных критериев совпадения
- Персонализированные рекомендации
- A/B тестирование промптов для LLM

## Структура проекта

```
├── src/
│   ├── bot.py              # Основная логика бота
│   ├── database.py         # SQLite интеграция
│   ├── vector_db.py        # Qdrant интеграция
│   ├── embeddings.py       # Создание эмбеддингов
│   ├── llm_service.py      # DeepSeek API
│   └── text_processing.py  # Обработка текста
├── logs/                   # Файлы логов
├── main.py                 # Точка входа
├── requirements.txt        # Зависимости
├── .env                    # Переменные окружения
└── README.md              # Документация
```